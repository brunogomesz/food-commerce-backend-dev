npm init -y
npm i -D @types/node nodemon rimraf ts-node typescript // bibliotecas iniciais que são necessárias bibliotecas para transpilar o typescript em javascript
npx tsc --init // cria o arquivo tsconfig.json
npx tsc // vai executar toda a parte build, e vai gravar no diretório index.js



mudar o arquivo do outDir no tsconfig.json para "./build

a parte de script para automatizar os comandos 

npm run + a chave que está no script (package.son)

rimraf apaga o diretório e vai executar novamente tsc para gravar algo novo lá

nodemon serve para detectar mudanças na pasta src e executa o comando build automaticamente, para criar isso, cria-se um novo arquivo chamado nodemon e coloca algumas informações no mesmo

npm run dev